spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/ai_trading_db
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  ai:
    ollama:
      base-url: http://localhost:11434

      chat:
        options:
          model: llama3.1:8b
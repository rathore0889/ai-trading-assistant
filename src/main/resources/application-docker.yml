spring:

  datasource:
    url: jdbc:postgresql://postgres:5432/ai_trading_db
    username: postgres
    password: root

  data:
    redis:
      host: redis
      port: 6379

  kafka:
    bootstrap-servers: kafka:9092

  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  ai:
    ollama:
      base-url: http://host.docker.internal:11434

      chat:
        options:
          model: qwen2.5:3b

market:
  api:
    key: d97skcpr01qng2npe8qgd97skcpr01qng2npe8r0
    url: https://finnhub.io/api/v1